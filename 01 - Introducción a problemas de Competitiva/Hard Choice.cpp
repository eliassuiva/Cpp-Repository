
/********************************************************************
SUIVA ELÍAS JOSÉ
C++ beecrowd | 2702
Hard Choice
********************************************************************/
/*
#include <iostream>
#include <cmath>

int main() {

    int polloDisponible, carneDisponible, pastaDisponible, polloPedido, carnePedido, pastaPedido;
    int genteSinSuPedido, diferenciaPollo, diferenciaCarne, diferenciaPasta;


    while (std::cin >> polloDisponible >> carneDisponible  >> pastaDisponible  >> polloPedido  >> carnePedido  >> pastaPedido) {

        diferenciaPollo = 0;
        diferenciaCarne = 0;
        diferenciaPasta = 0;

        if (polloDisponible<polloPedido) {
            diferenciaPollo = polloPedido - polloDisponible;
        }
        if (carneDisponible<carnePedido) {
            diferenciaCarne = carnePedido - carneDisponible;
        }
        if (pastaDisponible<pastaPedido) {
            diferenciaPasta = pastaPedido - pastaDisponible;
        }

        genteSinSuPedido = diferenciaPollo + diferenciaCarne + diferenciaPasta;
        std::cout << genteSinSuPedido << std::endl;
    }





    return 0;
}
*/